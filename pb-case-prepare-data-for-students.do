/******************************************************************************************************File        : pb-case-prepare-data-for-students.do  <--- this should be the exact name of THIS documentAuthor      : Kristina Tobio Created     : 19 Jan 2017Modified    : 19 Jan 2017Description : .do file for preparing data for use in PA_pilgrimB_code.do and PA_pilgrimC_code.do.******************************************************************************************************/// these commands prepare your computer for the data and analysis// this finds and closes open log filescapture log close        // this clears Stata's data memory so new data can be loadedclear       // this makes it so you don't have to keep pressing return/enter to scroll through resultsset more off          // this keeps everything visible on a normal monitor or laptop screenset linesize 200    // cd means "change directory"// changes to the current directory cd .// this creates a log file, which will record all of the commands and outputs from this .do file // log files should be placed in a folder named "logs" in your directory log using "logs/pb-case-prepare-data-for-students", replace// Merge together the new variables created by Part A plus the Part B case variables to create a dataset for Part Buse "data/PA_pilgrimB_original_data.dta"merge 1:1 id using "data/PA_pilgrimA_updated_data.dta"tab _mergedrop _mergesave "data/PA_pilgrimB_data.dta", replaceexport excel "data/PA_pilgrimB_data.xls", replace firstrow(variables)clear// Merge together the new variables created by Part A and Part B plus the Part C case variables to create a dataset for Part Cuse "data/PA_pilgrimC_original_data.dta"merge 1:1 id using "data/PA_pilgrimB_updated_data.dta"tab _mergedrop _mergesave "data/PA_pilgrimC_data.dta", replaceexport excel "data/PA_pilgrimC_data.xls", replace firstrow(variables)